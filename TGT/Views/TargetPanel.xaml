<UserControl x:Class="TGT.Views.TargetPanel" x:Name="TargetPanelRoot"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:TGT.ViewModels"
             xmlns:views="clr-namespace:TGT.Views">

    <!-- 여기서는 DataContext 설정 X (code-behind에서 함) -->

    <Grid Background="{StaticResource TargetPanelBrush}" Margin="4">
        <Border Background="{StaticResource TargetPanelBrush}" CornerRadius="8" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- 일괄발사 버튼 -->
                    <RowDefinition Height="*"/>
                    <!-- 표적 카드 리스트 -->
                    <RowDefinition Height="Auto"/>
                    <!-- 표적 생성부 -->
                </Grid.RowDefinitions>

                <!-- 상단 일괄발사 버튼 -->
                <Button Content="일괄 발사"
                    Grid.Row="0"
                    Command="{Binding StartAllCommand}"
                    Style="{StaticResource StartAllButtonStyle}"/>

                <!-- 표적 카드 리스트 -->
                <ScrollViewer Grid.Row="1"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              Margin="0,4,0,4">
                    <ItemsControl ItemsSource="{Binding Targets}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <views:TargetCard />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>

                        <!-- 표적이 없을 때 표시되는 메시지 -->
                        <ItemsControl.Style>
                            <Style TargetType="ItemsControl">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Targets.Count}" Value="0">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <TextBlock Text="등록된 표적이 없습니다."
                                                               Foreground="Gray"
                                                               FontStyle="Italic"
                                                               HorizontalAlignment="Center"
                                                               VerticalAlignment="Center"
                                                               Margin="0,20,0,0"/>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ItemsControl.Style>
                    </ItemsControl>
                </ScrollViewer>

                <!-- 표적 생성 패널 -->
                <views:TargetCreationPanel Grid.Row="2" Margin="0,10,0,0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
